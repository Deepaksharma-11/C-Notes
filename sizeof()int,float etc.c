#include <stdio.h>

int main() {
    int a;
    char b;
    float c;
    double d;
    printf("size of int: %d\n",sizeof(a));
    printf("size of chat: %d\n",sizeof(b));
    printf("size of float: %d\n",sizeof(c));
    printf("size of double: %d\n",sizeof(d));

    return 0;
}